spring.application.name=ContactManager
# Server Configuration
server.port=8080

# Database Configuration (Uses env variables, defaults to localhost)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/contact_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Akshay@9705}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect